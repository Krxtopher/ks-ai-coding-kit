# Ideas & Future Plans

Notes on features and improvements to build out over time.

## Installation CLI — ✅ Implemented

The catalog-driven installer is live. See `install.py` and `catalog.yaml` at the repo root.

### Remaining Ideas

- **Automated README generation** from `catalog.yaml` metadata
- **Packaging** as a pip-installable CLI (`ai-ext`) for easier distribution

## Compatibility Notes Reminder

When adding new items to this repo, always include a **Compatibility** note indicating which AI coding tools the item supports. See `AGENTS.md` for the format and valid tool names.
